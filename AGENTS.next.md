# 全局工作约定

## 沟通
- 使用中文回复，代码、命令、标识符保持英文
- 回答简洁，不重复我已经知道的信息
- 需求不明确或有多种合理方案时，先问再做，不要猜
- 完成任务后用 3 行以内总结改了什么、为什么

## 工作方式
- 涉及 3 个以上文件或架构层面的改动，先给出计划等我确认
- 修改代码后运行相关测试和类型检查，把结果告诉我
- 不要主动 commit / push，除非我明确说
- 不要为了让测试通过而删除或跳过测试
- 引入新依赖前先说明理由并征求同意
- 遇到失败不要反复盲试，超过 2 次同类错误就停下来分析

## 环境
- macOS，zsh，Homebrew
- 优先使用：`rg`（搜索）、`fd`（找文件）、`gh`（GitHub 操作）
- Node 用 pnpm，Python 用 uv，运行时通过 mise 管理
- 公司网络需走代理，npm / pip 已配置镜像，不要修改源

## 安全红线（任何项目都适用）
- 禁止：`rm -rf` 目录、`git push --force`、`git reset --hard` 到已推送的提交
- 禁止读取或输出 `.env*`、`*.pem`、`~/.ssh/`、任何 token / 密钥
- 禁止对数据库执行 DROP / TRUNCATE / 无 WHERE 的 UPDATE、DELETE
- 禁止修改 `~/.zshrc`、`~/.gitconfig` 等全局配置
- 执行任何不可逆操作前先确认

## 编码偏好（项目级配置优先）
- 错误要处理或明确向上抛，不要静默吞掉
- 修改现有代码时遵循该文件已有风格，不要顺手重构无关部分
- 不写无意义注释，只解释"为什么"而不是"做了什么"

## 优先级
- 项目目录下的 AGENTS.md / CLAUDE.md 与本文件冲突时，以项目文件为准
- 对话中的明确指令优先级最高
